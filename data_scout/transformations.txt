Aggregate Functions
   KTHLARGEST Function
   KTHLARGESTUNIQUE Function

Nested Functions
    ARRAYCROSS Function

Other Functions
    SOURCEROWNUMBER Function


Move transformations to a separate package
    - Export code
Paging for recipes and data sources
Tour
File upload
SQL connector
Excel connector

Improved logging (separated from serving)
Join datasets/recipes
Help function
Improve logout timer
Store data schemas
Return result until an error in the recipe
Multitype input (INPUT_value, INPUT_type)
Spark support

Automatically build the menu
Custom transformations
    Hook system?
Marketplace
    Rpository systeem -> CSV file oid.
    Installation command -> e.g. pip install ...
    Store installed packages in DB
        Package name
        Call method (eval?)

Data quality checks
    Types
    Bias
    Drift
    etc.