Aggregate Functions
   KTHLARGEST Function
   KTHLARGESTUNIQUE Function

Nested Functions
    ARRAYCROSS Function

Other Functions
    SOURCEROWNUMBER Function


Multitype input (INPUT_value, INPUT_type)

Return human-readable filename when outputting code

Input validation for transformations

Only allow users to see their own projects, data sources, etc.
 - Add project ids to all elements
 - Add IAM roles

Make it possible to view as read only

Return result until an error in the recipe

Write tests


Documentation

Spark support
Help function

Automatically build the menu
Custom transformations
    Hook system?
Marketplace
    Repository system -> CSV file oid.
    Installation command -> e.g. pip install ...
    Store installed packages in DB
        Package name
        Call method (eval?)

Tour

Data quality checks
    Types
    Bias
    Drift
    etc.